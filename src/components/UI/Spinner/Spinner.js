import React from 'react';

import classes from '../../UI/Spinner/Spinner.module.css';

const Spinner = (props) => (
    
    <div className = {classes.Loader}>Loading...</div>
);

export default Spinner;